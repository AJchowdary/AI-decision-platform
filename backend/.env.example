# Supabase (Project Settings → API)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: direct Postgres URL (same DB as Supabase)
# DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# Required for insights + decision cards (OpenAI)
OPENAI_API_KEY=sk-...

# Optional: extra CORS origins (comma-separated). Required in production — set to your frontend URL(s).
# ALLOWED_ORIGINS=https://your-app.vercel.app,https://www.yourdomain.com

# Optional: frontend URL for Stripe checkout redirects (success/cancel). Defaults to localhost if unset.
# FRONTEND_BASE_URL=https://your-app.vercel.app

# Billing: Stripe ($20/month after 14-day trial). Create price in Stripe Dashboard, then webhook for customer.subscription.updated/deleted
# STRIPE_SECRET_KEY=sk_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_ID=price_...

API_HOST=0.0.0.0
API_PORT=8000
